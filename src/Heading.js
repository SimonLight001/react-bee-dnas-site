import spacesLogo from './icons/spaces_logo.svg';

const Heading = () => {
    return (
        <div className="heading">
            <img src={spacesLogo} alt="Spaces Logo" />
        </div>
    );
}

export default Heading;